import { StyleSheet } from "react-native";

const CharactersScreenStyle = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: 'gray',
    },
});

export default CharactersScreenStyle;